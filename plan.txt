1) codepan or jsbin with code example - githubpages / vercel
2) compareArrays - more correct responses 
3) objKeyExists -> objLevelKeyExists

5) get each 2.3.5 element from array fb(arr, eachNum). "Децимація" – загальний термін для вибору кожного n-ного елемента.
const arr = [1, 2, 3, 4, 5, 6, 7, 8, 9];
const everyThird = arr.filter((_, index) => (index + 1) % 3 === 0); console.log(everyThird); // [3, 6, 9]
const everyFifth = arr.filter((_, index) => (index + 1) % 5 === 0); console.log(everyFifth); // [5]

- add fns from prj-slavic-names for read-parse-handle-write
створить свій модуль щоб принтувать данні - pokaz

how to create your own function to group objects in an array by one property.

- two array to object
https://stackoverflow.com/questions/39127989/create-an-object-from-an-array-of-keys-and-an-array-of-values

uniqueElArray - add checker type. fn must work only with: 
Single-type arrays, Typed arrays, Homogeneous arrays

getDuplicates and uniqueElArray

explodeArrayInterleave / joinArraysInterleave - adapting for array with diff length